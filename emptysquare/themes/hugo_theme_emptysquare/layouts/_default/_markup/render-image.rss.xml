{{/* Email-safe output for Atom/Kit */}}
{{/* .IsBlock is available if configured: gohugo.io/render-hooks/images/ */}}
{{- if .IsBlock -}}
<img src="{{ .Destination | safeURL }}" style="display: block; max-width:100%" alt="{{ .Text }}" title="{{ .Title }}">
{{- else -}}
<img src="{{ .Destination | safeURL }}" style="display:inline;width:auto;height:auto;vertical-align:middle" alt="{{ .Text }}" title="{{ .Title }}">
{{- end -}}
